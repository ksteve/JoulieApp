<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <EditTextPreference
        android:capitalize="none"
        android:inputType="numberDecimal"
        android:key="mid_peak_cost"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="Mid Peak Cost (cents/kWh)"
        android:summary="This will allow Joulie to give you an estimated usage cost"/>

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
   <!--<ListPreference-->
        <!--android:defaultValue="0"-->
        <!--android:entries="@array/power_usage"-->
        <!--android:entryValues="@array/power_usage"-->
        <!--android:key="sorting_list"-->
        <!--android:negativeButtonText="@null"-->
        <!--android:positiveButtonText="@null"-->
        <!--android:summary="%s"-->
        <!--android:title="@string/pref_title_power_usage" />-->

</PreferenceScreen>
